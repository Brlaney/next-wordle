import styles from '@/styles/components/ModalDetail.module.scss';

export default function ModalDetail() {
  return (
    //   <img src={`/${image}.jpeg`} className={styles['modal-details']} />
    <div className={styles['modal-detail']} />
  );
}
